- content_for :page_title, "Sign up"

= render "shared/landing_header"

= render "layouts/sm", title: "Almost there!", back_href: "/signup" do
	Check your email
	%span.text-gray-400 (#{@email})
	for a verification link! 🔗
